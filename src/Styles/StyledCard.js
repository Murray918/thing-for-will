import styled from "styled-components";
import Card from "@material-ui/core/Card";

const StyledCard = styled(Card)`
  .mma-card-actions-container {
    margin-right: 2rem;
  }
`;

export default StyledCard;
